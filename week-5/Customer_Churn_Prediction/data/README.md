# Data

Place the real IBM/Kaggle Telco Customer Churn CSV here as `Telco-Customer-Churn.csv`.
If it is absent, `main.py` generates a clearly labelled seeded synthetic dataset so the project remains runnable offline.
