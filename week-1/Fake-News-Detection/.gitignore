# Byte-compiled / cache files
__pycache__/
*.py[cod]
*.pyc

# Virtual environments
venv/
env/
.venv/

# Jupyter checkpoints
.ipynb_checkpoints/

# OS files
.DS_Store
Thumbs.db

# Generated intermediate data (large; fully reproducible via `python src/train.py`)
# The raw dataset (data/raw/) and trained model files (models/*.pkl) ARE
# tracked in git so the project runs immediately after cloning, without
# requiring a training step first.
data/processed/*.csv

# Streamlit
.streamlit/secrets.toml

# Editor/IDE
.vscode/
.idea/
